# react-virtual-paginated-select

**大数据渲染（虚拟列表 + 分页下拉 + 编辑回显）**

适用场景：8000+ 条数据的高性能渲染

关键词：大数据、虚拟列表、虚拟滚动、分页下拉、远程搜索、编辑回显、8000、万级、高性能、react-window、virtual、paginated、select、属性值、分类选择

排除词：vue、element、标准列表、导入

## 关键点

- 虚拟列表必须设置固定行高
- 分页下拉需实现选中项合并逻辑（编辑回显）
- 搜索必须防抖处理（300-500ms）
