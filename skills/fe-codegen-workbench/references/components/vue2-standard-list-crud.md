# vue2-standard-list-crud

**标准列表页（Element UI + Options API）**

适用场景：Vue 2 标准 CRUD 列表页

关键词：列表、列表页、表格、搜索、查询、新增、编辑、删除、批量、getwayTable、vue2、options

排除词：导入、xlsx、excel、详情页、单据详情、vue3

使用 `el-table` + `el-dialog` + 分页。
